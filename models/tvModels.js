class Television{
    constructor(price, brand, size, available, bought, state, broken){
        this.price = price;
        this.brand = brand;
        this.size = size;
        this.available = available;
        this.bought = bought;
        this.state = state;
        this.broken = broken;
    }
}

module.exports = Television;